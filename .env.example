# MongoDB Configuration
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=changeme

# Note: After changing these values, you need to rebuild the containers
# with: docker-compose down -v && docker-compose up --build
